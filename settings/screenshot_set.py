# Настройки для TradingView
# размеры окна для скрина (viewport в Playwright)
win_x = 1480
win_y = 1015  # было 935, добавлено ~80px для компенсации разницы Selenium vs Playwright
# координаты QR-оверлеев на FIN-скрине
qr110_x = 0     # QR110 по горизонтали
qr110_y = 640   # QR110 по вертикали
qr85_x = 1348   # QR85 по горизонтали
qr85_y = 850    # QR85 по вертикали
# Настройки для Pocket
# размеры окна для скрина (viewport в Playwright)
win_x_otc = 1712
win_y_otc = 990  # было 910, добавлено ~80px для компенсации
# координата QR на OTC-скрине (один QR — qr-code_110); подобрано под binodex-канвас 1452×870
otc_qr_x = 1360
otc_qr_y = 757


# ── Статичные картинки-оверлеи ────────────────────────────────────────────────────────────
# Кэш и вставка оверлеев (глобус/QR/водяной знак) переехали в общий пакет binocore.images
# (pip из git, версия ПРИПИННЕНА в requirements.txt — ядро обновляется вместе с программой,
# а не когда кто-то запушил в общую репу). Здесь остался только реэкспорт: места вызова по
# всей программе не меняются, а settings/__init__ раздаёт эти имена дальше через
# `from .screenshot_set import *`.
# configure — под подчёркиванием, чтобы не уезжать в этот `import *`; сам вызов живёт в
# apps/app.py: звать его отсюда нельзя, settings импортируется раньше logs (круг импортов).
from binocore.images import load_rgba, paste_overlay  # noqa: F401
from binocore.images import configure as _configure  # noqa: F401
